package Entity.InsertPatient;

import java.io.File;

import java.net.MalformedURLException;
import java.net.URL;

import java.util.logging.Level;
import java.util.logging.Logger;

import javax.xml.namespace.QName;
import javax.xml.ws.Service;
import javax.xml.ws.WebEndpoint;
import javax.xml.ws.WebServiceClient;
import javax.xml.ws.WebServiceFeature;
// !DO NOT EDIT THIS FILE!
// This source file is generated by Oracle tools
// Contents may be subject to change
// For reporting problems, use the following
// Version = Oracle WebServices (11.1.1.0.0, build 091026.1130.19937)

@WebServiceClient(wsdlLocation="http://ORACLE-SOA-SUITE:8001/soa-infra/services/default/Project1/InsertPatient?WSDL",
  targetNamespace="http://oracle.com/sca/soapservice/epd/EPDSOA/InsertPatient",
  name="InsertPatient")
public class InsertPatient
  extends Service
{
  private static URL wsdlLocationURL;

  private static Logger logger;
  static
  {
    try
    {
      logger = Logger.getLogger("Entity.InsertPatient.InsertPatient");
      URL baseUrl = InsertPatient.class.getResource(".");
      if (baseUrl == null)
      {
        wsdlLocationURL =
            InsertPatient.class.getResource("http://ORACLE-SOA-SUITE:8001/soa-infra/services/default/Project1/InsertPatient?WSDL");
        if (wsdlLocationURL == null)
        {
          baseUrl = new File(".").toURL();
          wsdlLocationURL =
              new URL(baseUrl, "http://ORACLE-SOA-SUITE:8001/soa-infra/services/default/Project1/InsertPatient?WSDL");
        }
      }
      else
      {
                if (!baseUrl.getPath().endsWith("/")) {
         baseUrl = new URL(baseUrl, baseUrl.getPath() + "/");
}
                wsdlLocationURL =
            new URL(baseUrl, "http://ORACLE-SOA-SUITE:8001/soa-infra/services/default/Project1/InsertPatient?WSDL");
      }
    }
    catch (MalformedURLException e)
    {
      logger.log(Level.ALL,
          "Failed to create wsdlLocationURL using http://ORACLE-SOA-SUITE:8001/soa-infra/services/default/Project1/InsertPatient?WSDL",
          e);
    }
  }

  public InsertPatient()
  {
    super(wsdlLocationURL,
          new QName("http://oracle.com/sca/soapservice/epd/EPDSOA/InsertPatient",
                    "InsertPatient"));
  }

  public InsertPatient(URL wsdlLocation, QName serviceName)
  {
    super(wsdlLocation, serviceName);
  }

  @WebEndpoint(name="execute_pt")
  public Entity.InsertPatient.Execute_ptt getExecute_pt()
  {
    return (Entity.InsertPatient.Execute_ptt) super.getPort(new QName("http://oracle.com/sca/soapservice/epd/EPDSOA/InsertPatient",
                                                                      "execute_pt"),
                                                            Entity.InsertPatient.Execute_ptt.class);
  }

  @WebEndpoint(name="execute_pt")
  public Entity.InsertPatient.Execute_ptt getExecute_pt(WebServiceFeature... features)
  {
    return (Entity.InsertPatient.Execute_ptt) super.getPort(new QName("http://oracle.com/sca/soapservice/epd/EPDSOA/InsertPatient",
                                                                      "execute_pt"),
                                                            Entity.InsertPatient.Execute_ptt.class,
                                                            features);
  }
}
